import React from 'react';

const Viewport = () => {
  return (
    <meta
      name="viewport"
      content="initial-scale=1, maximum-scale=5, width=device-width, shrink-to-fit=no"
    />
  );
};

export default Viewport;
